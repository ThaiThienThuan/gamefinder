# GameMatch monorepo env files live PER-MODULE, not at root.
#
# Backend env  -> server/.env       (template: server/.env.example)
# Frontend env -> client/.env       (template: client/.env.example)
#
# DO NOT create a real .env file at the repo root.
